export type Note = {
    id: number;
    title: string;
    text: string;
    backGroundColor: string;
  }

  
export type List = {
  id:number;
  title: string;
  list: string[];
  backGroundColor: string;
}